// Copy the contents of this file into config.js and fill it out with
// the correct info

const config = {
    STEAM_API_KEY: "YOUR_STEAM_API_KEY",
    host: "localhost",
    port: 8000
};

module.exports = config;